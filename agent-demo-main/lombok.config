# 第六刀 Batch 2: 启用 Lombok 把字段上的 @Qualifier 自动复制到 RequiredArgsConstructor 生成的构造器参数上
# 这样 4 个 Answer Node 用 @Qualifier 区分注入不同的 ChatClient bean 时, 仍可继续用 @RequiredArgsConstructor
# 参考: https://projectlombok.org/features/experimental/onX
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier